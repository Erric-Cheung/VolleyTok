import { createUser } from "@/lib/actions/user";

// After registration user redirected here and user is created ?
export default async function Auth() {

  // created in server
  return <div>Authorizing...</div>;
}
